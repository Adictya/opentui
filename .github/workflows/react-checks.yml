name: React Package Checks

on:
  pull_request:
    paths:
      - "packages/react/**"
    branches: [main]

jobs:
  react-prettier:
    name: React - Prettier Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Prettier formatting
        run: npx prettier --check packages/react

  react-build:
    name: React - Build and Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build core package (required for react types)
        run: |
          cd packages/core
          bun run build:lib

      - name: Type check
        run: |
          cd packages/react
          bun run build
